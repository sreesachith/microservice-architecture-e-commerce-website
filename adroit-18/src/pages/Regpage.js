// This is Regpage.js
import Register from "../components/Register.js";

// Define a functional component for the registration page
function RegPage() {
    return (
        <main>
            <Register/>
        </main>
    );
}

// Export it as the default export of this module
export default RegPage;
